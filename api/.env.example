# Copy to .env and fill in values
# .env is gitignored — never commit real secrets

DJANGO_SECRET_KEY=replace-with-a-real-secret-key
DATABASE_URL=postgres://user:password@localhost:5432/renovaite

# Magic link auth
MAGIC_LINK_BASE_URL=http://localhost:5173
MAGIC_LINK_EXPIRY_MINUTES=15
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend  # prod
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
