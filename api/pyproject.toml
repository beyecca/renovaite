[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "django>=5.0",
  "django-ninja>=1.1",
  "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
  "pytest",
  "pytest-django",
]
